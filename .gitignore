__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
dist/
build/
node_modules/
seoagents/dashboard/static/app/
.DS_Store
.claude/settings.local.json
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm
.env
*.tsbuildinfo
Docs/*.json

# Backups (local only)
backups/
*.bak
*.bak-*

# Runtime databases
*.db
*.db-wal
*.db-shm

# local dev virtualenvs
.venv*/
venv/

# 运行时密钥文件 —— 只有 .env 一条规则挡不住 .env.llm / .env.local 这类
.env.*
!.env.example
*.env
.env.llm

# 凭据目录 —— 2026-08-13 合并 PR#9 前发现未忽略（含 gsc-sa.json 私钥/
# google-drive-token.json/pagespeed-api-key.txt）。红线：凭据不进版本库。
credentials/
*-sa.json
*-oauth.json
*-token.json
*api-key*.txt

# 构建产物与运行时数据（每次构建/每日工作流都会变，跟踪它们会让服务器
# 工作区永远"脏"，git pull 被拒、真实改动被噪声淹没 —— 2026-08-18 G1-F
# 部署时就因此差点丢掉 G1-B/G1-H 只落在服务器的代码）
seoagents/dashboard/static/app/index.html
seoagents/dashboard/static/preview/seo-control-tower-sites.json
