"""Back-compat shim — the data-status contract moved to :mod:`dojocore.quality`.

It is framework, not SEO: every department needs the same guarantee that a
number carries its own trustworthiness.
"""
from dojocore.quality import *
